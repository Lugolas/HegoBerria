{% extends 'sitebdeSiteVitrineBundle::vueMereSiteVitrine.html.twig' %}

{% block navbar %}
    <div class="row">
        <div class="col-sm-10">
            <nav class="navbar navbar-default" role="navigation">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                    </div>
                
                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            <li><a href="{{ path('sitebde_siteVitrine_accueil') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a></li>
                            {% if typeArticle == "actualite" %}
                                <li class="active"><a href="{{ path('sitebde_siteVitrine_listeActualites') }}"><span class="sr-only">(current)</span>Actualités</a></li>
                                <li><a href="{{ path('sitebde_siteVitrine_listeEvenements') }}">Evènements</a></li>
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Informations <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        {% for infoCourante in informations %}
                                            <li><a href="{{ path('sitebde_siteVitrine_information', {'idInfo':infoCourante.id}) }}">{{ infoCourante.titre }}</a></li>
                                        {% else %}
                                            <li><a href="#">Il n'y a aucune information.</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                            {% elseif typeArticle == "evenement" %}
                                <li><a href="{{ path('sitebde_siteVitrine_listeActualites') }}">Actualités</a></li>
                                <li class="active"><a href="{{ path('sitebde_siteVitrine_listeEvenements') }}"><span class="sr-only">(current)</span>Evènements</a></li>
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Informations <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        {% for infoCourante in informations %}
                                            <li><a href="{{ path('sitebde_siteVitrine_information', {'idInfo':infoCourante.id}) }}">{{ infoCourante.titre }}</a></li>
                                        {% else %}
                                            <li><a href="#">Il n'y a aucune information.</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                            {% else %}
                                <li><a href="{{ path('sitebde_siteVitrine_listeActualites') }}">Actualités</a></li>
                                <li><a href="{{ path('sitebde_siteVitrine_listeEvenements') }}">Evènements</a></li>
                                <li class="dropdown active">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="sr-only">(current)</span>Informations <span class="caret"></span></a>
                                    <ul class="dropdown-menu">
                                        {% for infoCourante in informations %}
                                            <li><a href="{{ path('sitebde_siteVitrine_information', {'idInfo':infoCourante.id}) }}">{{ infoCourante.titre }}</a></li>
                                        {% else %}
                                            <li><a href="#">Il n'y a aucune information.</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                            {% endif %}
                        </ul>
                        <form class="navbar-form navbar-right">
                            <div class="form-group">
                                <input type="text" class="form-control" placeholder="Chercher">
                            </div>
                            <button type="submit" class="btn btn-default">Go !</button>
                        </form>
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="{{ path('sitebde_siteVitrine_connexion') }}">Se connecter</a></li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
        <div class="col-sm-2">
            <nav class="navbar navbar-default" role="navigation">
                <div class="container-fluid">
                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            <li><a href="{{ path('sitebde_parrainage_accueil') }}">Parrainage</a></li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </div>
{% endblock %}

{% block contenu %}

    <div class="row">
        {% if typeArticle == "actualite" %}
            <form action="{{ path('sitebde_siteVitrine_modifierActualite', {'id':article.id} ) }}">                <center>

        {% elseif typeArticle == "evenement" %}
            <form action="{{ path('sitebde_siteVitrine_modifierEvenement', {'id':article.id} ) }}">
        {% else %}
            <form action="{{ path('sitebde_siteVitrine_modifierInformation', {'id':article.id} ) }}">
        {% endif %}
                <center>
                    <button class="btn btn-primary" type="submit">
                        <h5>Editer</h5>
                    </button>
                </center>
            </form>
    </div>

    {% if typeArticle == "actualite" %}
        <div class="row">
            <div class="col-sm-2">
                <img src="{{ asset('images/iconesActualites/' ~ article.icone) }}" alt="Logo" class="img-rounded img-responsive">
            </div>
            <div class="col-sm-10">
                <h3>{{ article.titre }}</h3>
                {% if article.datePublication != article.dateActualisation %}
                    <p><small>Date d'actualisation : {{ article.dateActualisation|date("d/m/Y") }}
                     - Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
                {% else %}
                    <p><small>Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
                {% endif %}
                <p>{{ article.contenu }}</p>
            </div>
        </div>
    {% elseif typeArticle == "evenement" %}
        <div class="row">
            <div class="col-sm-2">
                <img src="{{ asset('images/iconesEvenements/' ~ article.icone) }}" alt="Logo" class="img-rounded img-responsive">
            </div>
            <div class="col-sm-10">
                <h3>{{ article.dateEvenement|date("d/m/Y") }} : {{ article.titre }}</h3>
                {% if article.datePublication != article.dateActualisation %}
                    <p><small>Date d'actualisation : {{ article.dateActualisation|date("d/m/Y") }}
                     - Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
                {% else %}
                    <p><small>Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
                {% endif %}
                <p>{{ article.contenu }}</p>
            </div>
        </div>
    {% else %}
        <h3>{{ article.titre }}</h3>
        {% if article.datePublication != article.dateActualisation %}
            <p><small>Date d'actualisation : {{ article.dateActualisation|date("d/m/Y") }}
             - Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
        {% else %}
            <p><small>Date de publication : {{ article.datePublication|date("d/m/Y") }}</small></p>
        {% endif %}
        <p>{{ article.contenu }}</p>
    {% endif %}
{% endblock %}